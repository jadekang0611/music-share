import { createTheme } from "@material-ui/core/styles";

const theme = createTheme({
  palette: {
    type: "dark",
    primary: {
      main: "#B1AFFF",
    },
    secondary: {
      main: "#C8FFD4",
    },
  },
});

export default theme;
